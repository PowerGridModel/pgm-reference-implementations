[tool.poetry]
name = "pgm-reference-implementations"
version = "0.1.0"
description = "Reference implementations and examples for Power Grid Model"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11,<3.14"
power-grid-model = "^1.12"
power-grid-model-io = "^1.3"
power-grid-model-ds = "^1.5"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
